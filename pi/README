
Install postgres, create database and tables

run server with

uvicorn main:app --reload --port 5000

create a service

sudo nano /etc/systemd/system/fastapi.service

[Unit]
Description=FastAPI Service
After=network.target

[Service]
User=cx
Group=cx
WorkingDirectory=/home/cx/path_to_your_fastapi_app
ExecStart=/home/cx/path_to_your_fastapi_app/.venv/bin/uvicorn main:app --host 0.0.0.0 --port 5000
Restart=always

[Install]
WantedBy=multi-user.target
